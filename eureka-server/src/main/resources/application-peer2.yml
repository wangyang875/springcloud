server:
  port: 8084
eureka:
  instance:
    hostname: peer2
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
     defaultZone: http://peer1:8080/eureka/
  server:
    enable-self-preservation: false